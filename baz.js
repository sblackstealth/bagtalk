function everyElem (arr, function(num)){
    
    var arr =[ 2 ,3 , 4, 6]
    
    for(var i = 0; i < arr.length; i++){
    
        var evenElementsOnly = arr.filter(function isEven(arr[i]) {
    
            return num % 2 === 0
    
        })
    
    }
    
}